# Setup report

Updated by Claude Code after every phase of setup/CLAUDE_CODE_SETUP.md. Cowork reads this file to follow progress.

## Pending handoff
>>> COWORK: Open a new PowerShell window and run: & "C:\Program Files\GitHub CLI\gh.exe" auth login --hostname github.com --git-protocol https --web  (press Enter to open the browser, have Jimmy sign in to GitHub and enter the one-time code shown, approve). When the window says "Logged in as <name>", type: done

## Phase log
| Phase | Status | What was done | Evidence |
|---|---|---|---|
| K1 Environment + first commit | PASS | doctor exit 0 (UE 5.8, Blender 5.2.2 LTS, VS 18.5, git 2.51 + LFS 3.7, uv 0.12.18, Claude Code 2.1.280, MCP port 8000). git init + lfs install; added `*.slnx` to .gitignore (generated solution files); first commit 8858e7c. 753 LFS files incl. 4 .umap; status clean; Saved/ ignored. | Saved/AgentLogs/status/doctor.json, tools/local.settings.json |
| K2 Unreal MCP acceptance | PASS | launch-editor exit 0. 52 toolsets listed (AllToolsets active). SceneTools.find_actors returned 72 actors in /Game/ThirdPerson/Lvl_ThirdPerson. **Fix:** Epic's ProgrammaticToolset.execute_tool_script is a sandbox with no `unreal` module, so added project toolset `vibegame_tools.VibeGamePipelineTools` (run_python / run_pipeline), registered at editor start by Content/Python/init_unreal.py; it printed engine 5.8.3-58210709+++UE5+Release-5.8, project dir C:/GameDev/VibeGame/. **Fix:** screenshots never rendered because the editor was CPU-throttled in the background (~2 fps); set bThrottleCPUWhenNotForeground=False (live + Config/DefaultEditorPerProjectUserSettings.ini); added pu.take_screenshot. Screenshot checked: Third Person template level. PythonStub present (bDeveloperMode=True). Updated unreal-pipeline skill, editor-operator agent, CLAUDE.md. Step 8: Cowork installed plugin unreal-engine-skills-for-claude-code (v3.1.1); skill `unreal-mcp` loaded after restart; unreal-mcp answered again (get_current_level). | Saved/AgentLogs/setup/unreal_toolsets.txt, Saved/AgentLogs/setup/k2_viewport.png, Saved/Logs/VibeGame.log (`VibeGamePipelineTools registered`) |
| K3 Blender MCP install | PASS | Cloned https://projects.blender.org/lab/blender_mcp.git (ff54e4d) to C:\GameDev\_tools\blender_mcp. Add-on built from addon/blender_mcp_addon (`extension build`, mcp-1.0.0.zip) and installed with `extension install-file -r user_default -e` ("STATUS Installed \"mcp\""; `extension list`: mcp [installed]). Server: uv venv mcp/.venv, `uv pip install -e .` (blender-mcp 1.0.2), entry mcp/.venv/Scripts/blender-mcp.exe. Registered in .mcp.json as `blender` (stdio, BLENDER_PATH = Blender 5.2 exe, port 9876); JSON valid; unreal-mcp entry unchanged. **Fix:** every background call timed out (120 s) under MCP stdio on Windows because Blender inherited the server's stdin pipe; local patch adds `stdin=subprocess.DEVNULL` in blmcp/tools_helpers/blender_cli.py (saved as setup/patches/blender_mcp_stdin_devnull.patch). Stdio smoke test after patch: 26 tools, execute_blender_code_for_cli returned version 5.2.2 LTS in 5 s. Made C:/GameDev/_tools/empty.blend for file-less background calls. | Saved/AgentLogs/setup/k3_blender_mcp_smoke.txt, setup/patches/blender_mcp_stdin_devnull.patch |
| K4 Blender MCP acceptance | PASS (live check WARN) | After restart the 26 `blender` MCP tools are available. execute_blender_code_for_cli (background) returned bpy.app.version_string = 5.2.2 LTS, binary C:/Program Files/Blender Foundation/Blender 5.2/blender.exe. Live check (optional) skipped = WARN: add-on MCP v1.0.0 installed and enabled (localhost:9876, Auto Start on) but its server stays stopped because Blender's "Allow Online Access" preference is off, and Jimmy chose to keep it off; live tools answer "Cannot connect to Blender at localhost:9876". Background mode (what the pipeline uses) is unaffected. | Claude Code session transcript |
| K5 Golden path | PASS (live route) | blender-run sm_golden_crate exit 0: FBX exported, RESULT dimensions 1.0 x 1.0 x 1.0 m, min z 0 (bottom-center pivot), 108 tris, material M_GoldenCrate; preview checked (brown beveled box). Live editor via vibegame_tools run_python: import_static_mesh -> /Game/Art/Props/SM_GoldenCrate, box_extent [50, 50, 50], origin z 50 (plus M_GoldenCrate); build_golden_level -> /Game/Maps/Dev/L_GoldenPath saved with GP_Sun, GP_SkyAtmosphere, GP_SkyLight, GP_Floor, GP_Crate. Screenshot via pu.take_screenshot checked: lit brown crate on a grid floor with a shadow. Headless fallback not needed. | art/export/Props/SM_GoldenCrate.fbx, Saved/AgentLogs/previews/SM_GoldenCrate.png, Saved/AgentLogs/blender/sm_golden_crate.result.json, Saved/AgentLogs/setup/golden_path.png |
| K6 C++ automation test | PASS | Added Source/VibeGame/Tests/GoldenPathTest.cpp (reference code; EditorContext/ProductFilter flag names confirmed in AutomationTest.h). Saved all via run_python (levels + /Game dirty assets), stop-editor exit 0 (no dialog). build.ps1 succeeded (GoldenPathTest.cpp compiled, 14 s). run-tests -Filter Project.GoldenPath: 1 passed, 0 failed (PASS Project.GoldenPath.CrateImported). launch-editor exit 0; list_toolsets answers (vibegame_tools registered again). | Saved/AgentLogs/build/build-20260922-211005.out.log, Saved/AgentLogs/tests/20260922-211024/ |
| K7 Guardrails + team | PASS | Hook test: Write of Saved/AgentLogs/setup/hooktest/Fake.uasset BLOCKED (file not created): "PreToolUse:Write hook error: [...guard-binary-assets.ps1]: BLOCKED: C:\GameDev\VibeGame\Saved\AgentLogs\setup\hooktest\Fake.uasset is a binary asset. Change Unreal assets through the editor (unreal-mcp, via the editor-operator subagent) or Unreal Python (Content/Python/pipeline_unreal.py); change Blender files through recipes in art/recipes. Never write them as text." Stop hook smoke test: exit=0. Subagents editor-operator, unreal-engineer, blender-artist, qa-tester load (listed as agent types); skills unreal-pipeline, blender-pipeline, verification, playtest-feedback present. qa-tester: PASS (run-tests.json + index.json: 1 succeeded, 0 failed, CrateImported state Success, 0 errors/warnings; screenshot = lit brown crate on grid floor with shadow; the icon it called a "loading spinner" at the top is the editor sprite of the GP_Sun directional light). blender-artist: PASS (exit 0, dimensions 1.0 x 1.0 x 1.0 m, min [-0.5, -0.5, 0.0], 108 tris, M_GoldenCrate, preview Saved/AgentLogs/previews/SM_GoldenCrate.png). | Saved/AgentLogs/tests/20260922-211024/index.json, Saved/AgentLogs/blender/sm_golden_crate-20260922-211130.out.log |
| K8 Wrap-up | PASS | doctor exit 0 (no problems, no warnings). Acceptance criteria AC1-AC9 filled in (all PASS; live Blender mode noted as WARN under AC5). Notes for Jimmy written. Final commit "Setup complete". | Saved/AgentLogs/status/doctor.json |

## Acceptance criteria
| ID | Criterion | Status | Evidence |
|---|---|---|---|
| AC1 | Toolchain: VS 2026 (or 2022 17.14+) C++ game workloads, UE 5.8, Blender 5.1+, Git + LFS, Claude Code, uv | PASS | doctor exit 0 in K1 and K8, no problems/warnings: VS 18.5, UE 5.8 (5.8.3), Blender 5.2.2 LTS, git 2.51 + LFS 3.7, Claude Code 2.1.280, uv 0.12.18. Saved/AgentLogs/status/doctor.json |
| AC2 | Project builds with tools/build.ps1 | PASS | K6 build succeeded incl. GoldenPathTest.cpp. Saved/AgentLogs/build/build-20260922-211005.out.log |
| AC3 | Editor starts with the Unreal MCP server listening; Python API stub exists | PASS | launch-editor exit 0 (K2, K6 relaunch); Intermediate/PythonStub/unreal.py present (bDeveloperMode=True) |
| AC4 | Claude Code: unreal-mcp connected, toolsets listed, Python execution works, Epic plugin installed | PASS | 52 toolsets (Saved/AgentLogs/setup/unreal_toolsets.txt); Python works through the project toolset vibegame_tools.VibeGamePipelineTools (Epic's execute_tool_script is sandboxed without `unreal`), engine 5.8.3; plugin unreal-engine-skills-for-claude-code 3.1.1 installed, skill unreal-mcp loaded |
| AC5 | Blender MCP (official Blender Lab server) registered as `blender`; background Python returns the Blender version | PASS | .mcp.json `blender` -> C:/GameDev/_tools/blender_mcp/mcp/.venv/Scripts/blender-mcp.exe; execute_blender_code_for_cli returned 5.2.2 LTS (needs local stdin patch, setup/patches/). Live mode WARN (Online Access off by choice) |
| AC6 | Golden path: recipe -> FBX + preview -> imported SM_GoldenCrate with box extent ~50 uu -> L_GoldenPath saved -> screenshot shows crate on floor | PASS | art/export/Props/SM_GoldenCrate.fbx, Saved/AgentLogs/previews/SM_GoldenCrate.png, box_extent [50, 50, 50], /Game/Maps/Dev/L_GoldenPath, Saved/AgentLogs/setup/golden_path.png |
| AC7 | Automation test Project.GoldenPath.CrateImported passes headless | PASS | 1 passed, 0 failed. Saved/AgentLogs/tests/20260922-211024/index.json |
| AC8 | Guardrails: binary-asset hook blocks a .uasset write; Stop hook runs; git repo with LFS tracking .uasset/.umap; commits present | PASS | Fake.uasset write BLOCKED (K7 row); Stop hook exit=0; 756 .uasset/.umap files in LFS incl. SM_GoldenCrate, M_GoldenCrate, L_GoldenPath; git log shows the phase commits |
| AC9 | Team: 4 subagents + 4 skills present; qa-tester and blender-artist delegations succeeded | PASS | .claude/agents (4), .claude/skills (4); qa-tester PASS, blender-artist PASS (K7 row) |

## Warnings / non-blocking issues
- K4: Blender MCP live mode unavailable by choice ("Allow Online Access" off in Blender, so the add-on socket server does not start). Background-mode tools and headless recipes work. To enable later: Blender > Edit > Preferences > System > Allow Online Access, then start the MCP add-on server.
- K3: the Blender MCP clone at C:/GameDev/_tools/blender_mcp carries a local Windows patch (setup/patches/blender_mcp_stdin_devnull.patch); re-apply after updating the clone.
- K2: Epic's ProgrammaticToolset.execute_tool_script cannot use the `unreal` module; all editor Python goes through the project toolset vibegame_tools.VibeGamePipelineTools (documented in the unreal-pipeline skill and the editor-operator agent). Editor background CPU throttling is turned off so screenshots render when the editor is not in front.
- A Blender window opened for the K4 live check may still be open; it can be closed without saving.

## Notes for Jimmy
Everything is set up and tested: Claude can now build 3D models in Blender, bring them into Unreal, place them in a level, take a screenshot to check the result, and run automatic tests, all without you touching any code. As a first proof, it made a simple 1-metre crate, put it in a small test level, and confirmed it has the right size and looks right on screen. Blender's live link stays off because you chose to keep Online Access disabled; that is fine, since all the real work runs in the background. The next step is a short design interview with you (task T-001 in docs/TASKS.md) about what kind of game you want, so Claude can start building your first playable version.
