# Test plan (owned by qa-engineer)

Map of each system to the tests that prove it, plus known gaps. Keep it current with every task.
Run everything: `tools/run-tests.ps1 -Filter Project` (prefix match on `Project.*`; the editor must be closed).
Test levels: U = unit (pure logic/data), D = data validation, I = integration (test world), F = functional/scenario (map + bot), P = playtester (PIE, manual-agent).

| System | Test path(s) | Level | Written by | Gaps |
|---|---|---|---|---|
| Pipeline: golden crate import | Project.GoldenPath.CrateImported | I | setup | none |
| Engine smoke: all maps enter PIE | Project.Maps.PIE (engine-provided) | F | Epic | none |
| Playtest feedback F8 (T-003): implementer tests | Project.Playtest.NoteWriting, .GitCommit, .FpsAverage, .SubsystemDefaults; bug-fix tests (1e4825b) .BlankRootRejected, .NonFiniteWrittenAsNull, .BadScreenshotStillSavesNote, .GitCommitValidation; round 2 (4f56d15) .UtcTimestamp, .CameraInNote, .GitWorktree, .FeedbackKeyCapture, .FeedbackKeyHandler, .NoteTextAndConfirmation | U | unreal-engineer | see QA rows |
| Playtest feedback F8 (T-003): independent tests (`Tests/PlaytestFeedbackQATest.cpp`, 41 tests) | Project.Playtest.QA. Text.{Empty, VeryLong, NonAsciiAndEmoji, QuotesBackslashesNewlines}; Json.{SchemaFieldsAndTypes, NoScreenshotNotReferenced, DefaultNoteIsValid, LargeWorldCoordinates, NonFiniteNumbersStayValid, FileIsUtf8}; Screenshot.PixelsMatchCapture; Folder.{NameFormat, NamesSortChronologically, SameSecondCollisions, NeverWritesIntoExistingFolder, CreatesMissingRoot, RelativeRootGivesAbsoluteFolder, RootWithTrailingSeparator, DefaultRootIsSavedPlaytest}; Failure.{RootIsAFile, RootUnderAFile, InvalidCharactersInRoot, EmptyRoot, PixelCountMismatch}; Fps.{NoSamplesIsZero, SingleSample, WindowExactlyFilled, WindowBoundary, CustomWindowHonored, NonPositiveWindowIsClamped, ZeroDeltaFrameIsFinite, HitchLowersAverage, HugeSpikeIsFiniteAndAgesOut, LongSessionStaysAccurate}; Git.{UnbornBranchIsUnknown, CrlfLineEndings, PackedRefsExactBranchMatch, LooseRefWinsOverPacked, MalformedRepoIsUnknown, WorktreeGitFileIsSafe}; Config.DefaultsMatchSpec | U | qa-engineer | see the round-2 QA row (gaps kept up to date there) |
| Playtest feedback F8 (T-003) round 2: independent tests (same file, +29, 70 QA tests total) | Project.Playtest.QA. Timestamp.{UtcFormat, StampNowIsOneInstant, FolderLocalJsonUtc, DerivedWhenUtcUnset}; Json.{CameraBlockSchema, CameraNonFiniteIsNull, DefaultNoteHasCameraAndTimestamp}; Text.{NormalizeLineEndings, MultiLineJsonRoundTrip, MaxLengthBoundary}; Rules.{F8OpensNoteInPlay, F8GoesToEditorWhenEjected, F8PassesThroughWhileTyping, F8PassesThroughWhenNotPlaying, HeldF8IsSwallowed, FeedbackKeyAllCombinations, NoteBoxKeys, HandlerOrder, HandlerIndependentOfListOrder, TextEntryWidgets, TeardownSave, Confirmation, ConfirmationNeverClaimsMissingScreenshot}; Screenshot.InvalidPixelsDroppedWithWarning; Git.{WorktreeRelativeGitdirAndPackedCommon, WorktreePerWorktreeRefsStayLocal, WorktreeBrokenIsUnknown, IsCommitHashBoundaries}; Config.ToastDurations. Git.WorktreeGitFileIsSafe tightened: a worktree with a branch checked out must give the real commit (commondir followed) | U | qa-engineer | (1) In-game flow, playtester (P): F8 in PIE opens the box even when the viewport is not focused, and never ejects. The facts the rules are fed are subsystem-only (protected, not unit-testable): ejected/simulate, focus in a text field or the console open, key repeat, focusing the viewport first. Also: screenshot before the box, pause while typing, Enter saves to the real Saved/Playtest, Shift+Enter makes a new line, Esc writes nothing, the game resumes either way, toast look and timing (designer). (2) The 2000-character cap is a private constant on the Slate box (`MaxNoteLength` in PlaytestFeedbackSubsystem.cpp). Only the save path is tested: 1999/2000/2001 chars are kept exactly. Seam requested: a public `FPlaytestFeedbackRules::MaxNoteLength` or a pure clamp rule. Until then the playtester checks that the box stops at 2000 and whether a CRLF line break counts as 1 or 2. (3) Teardown save (game ends with the box open): only the rule ShouldSaveOnTeardown is tested; the save in Deinitialize is covered by the playtester. (4) Shipping compile-out: not unit-testable, covered by the implementer's Shipping build. (5) Subsystem integration (BeginFeedback with no viewport, never pausing on network clients): not tested yet; add when co-op starts. Closed: round-1 gap (4) text trimming (now the pure rule NormalizeNoteText, tested) and round-1 gap (5) worktree commit "unknown" (B5 fixed in 4f56d15; now required by Git.WorktreeGitFileIsSafe). |
| Fish system (T-008 core): implementer tests (`Tests/FishSystemTest.cpp`, 18 tests) | Project.Fish. Data.{SourcesImport, Validation, ValidatorCatchesBadData, NewRowsNeedNoCode}; Roll.{Deterministic, FailsGracefully, RarityOdds, LuckShiftsRarity, ModifierAddsThenMultiplies, ExclusivityAndCap, WeightScaling, ValueRisesWithRarity, WorkedExample}; Instance.SaveRoundTrip; Bite.PickSpecies; Helpers; Level.DifficultyMultiplier; Settings.TablesAndMissingAssets | U, D | unreal-engineer | see QA row |
| Fish system (T-008 core): independent tests (`Tests/FishQA*.cpp` + `FishQATestHelpers.h`, `FishQATestTags.cpp` for the test-only tags `Test.Fish.*` and `Fish.Stat.QA_TestOnly`; 121 tests; design `Saved/AgentLogs/qa/eng2-T008-test-design.md`) | Project.Fish.QA. Roll.{SameSeedSameInstance, SeedEdgeValues, DifferentSeedsVary, BatchOrderIndependent, IgnoresGlobalRandom, RerollFromStoredSeed, ValidDataIsSilent, UnknownSpeciesFailsGracefully, MissingTablesGraceful, MatchesSpecFormula_Real, MatchesSpecFormula_Fixture, StageIsolation, ParallelMatchesSerial, ForcedOverrides, StageSeedsMatchSpec, MakeRandomSeedVaries}; Rarity.{OddsMatchWeights_Real, OddsMatchWeights_Fixtures, PickWeightedIndexBoundaries, ZeroWeightNeverRolls, SingleTierAlwaysRolls, AllZeroWeightsGraceful, NegativeWeightTreatedAsZero, SpeciesAllowListRespected, LuckMatchesFormula, LuckExtremesSafe, LuckIsPathwiseMonotone, LuckDoesNotChangeModifiers, IndependentOfWeightAcrossSequentialSeeds}; Weight.{WithinSpeciesRange, CoversRange, DistributionMatchesSpec, MinEqualsMaxIsFinite, InvalidRangeGraceful, ModifierWeightRule, SampleWeightEdges}; Modifier.{AddsSum, MultipliesCompound, AddsBeforeMultiplies, RowOrderInvariant, MultipleStatsIndependent, NewStatViaData, ChanceZeroNeverOneAlways, ChanceMatchesOdds, NoDuplicates, ExclusiveGroupAtMostOne, ExclusiveGroupPartialChance, DifferentGroupsCoexist, UngroupedNotExclusive, IneligibleDoesNotBlockGroup, SpeciesConditionRespected, RegionTimeWeatherConditions, SpeciesAllowListIntersects, CountCapRespected, RarityAppliesBeforeModifiers}; Guard.{MultiplyByZeroSkipped, NegativeMultiplySkipped, NonFiniteModSkipped, LargeNegativeAddClampsToFloor, HugeMultiplyStaysFinite, NonFiniteRowValues, ValueOverflowSaturates}; Clamp.StatRanges; Value.{RarityUpValueUp, WeightUpValueUp, DominanceOnRealData, DependsOnlyOnRecord, DifficultyRisesWithRarity, XpFormulaAndMonotone, RoundsHalfUpMinimumOne}; Instance.{GetStat, HasModifier, DefaultIsEmpty, StatsCoverRegistry, IdentityPipeline, DanglingIdsGraceful, LevelRule}; Picker.{OnlyEligibleSpecies, WrongHabitatTimeOrBaitGivesNone, EmptyCandidatesGraceful, TimeWindowBoundaries, BaitMatchSemantics, RegionAndWeather, OddsFollowBiteWeights, DeterministicAndOrderIndependent, StarterDataWhereAndWhen}; Level.{SignAndNeutral, Monotone, ExtremesBounded, ShiftInvariant, SettingsDriveTheHook}; Data.{SourcesExistAndParse, AllTableSourcesParse, UniqueIds, TagsRegistered, TagNamespaces, ReferencesResolve, RangesSane, EverySpeciesCanRoll, RarityOrderConsistent, ExclusivityGroupsMeaningful, JournalInfoComplete, StarterContentPresent, AssetMatchesSource, ValidatorAgrees}; Extensibility.{NewRowsNeedNoCode, RenamedRowsWork, ManyTiersAndModifiers}; Scale.{ThousandSpeciesCorrect, ThousandSpeciesPerformance, LinearNotQuadratic, ManyModifiersPerformance}; Save.{StructRoundTrip, SaveGameFlagRoundTrip, ReflectionLint, LoadDoesNotRecompute, CoolerArrayRoundTrip}; Net.{RepLayoutRoundTrip, SizeBudget} | U, D, I | qa-engineer | Method: black-box from docs/specs/fish-system-rules.md and the header contracts; two independent oracles of the documented pipeline match Roll() field by field (18,000 starter-data rolls + 9,800 rich-fixture rolls, 0 mismatches); odds use fixed sequential seeds, 100k samples, 4.5 sigma per category + chi-square at alpha 1e-4 (deterministic, never flaky); the data tests restate the rules independently and feed 60 bad-row fixtures (+ all-null tables) and 9 legal edge cases to FFishDataValidator. Gaps: (1) real client/server replication (the Net tests cover the FRepLayout serialization path only; Iris not enabled); (2) server-only invocation of the roll (needs the T-006 bite flow); (3) T-007 reel fight reading instance stats, T-010 selling/XP, T-011 journal and T-019 save files carrying the instance: add integration tests with those tasks; (4) `Data.AssetMatchesSource` only compares when /Game/Data/DT_Fish* exist, i.e. in the main checkout after the editor-operator's import (in lanes it logs "skipped"): the lead confirms it there; (5) save compatibility across future FFishInstance layout changes (golden-bytes fixture, design QA-113) not added yet; (6) P3 not applicable: QA-95 "modifiers never lower difficulty" (Giant lowers Speed by design, Q28) and QA-115 custom NetSerialize (none; a static_assert in Net.RepLayoutRoundTrip asks for the test if one is added). |

### Open bugs (failing tests; each test is the regression test for its bug)
- FIXED in f39eb84 (GameplayTags fast replication; a typical catch now 122 B): T008-B1 `Project.Fish.QA.Net.SizeBudget` (found 2026-09-22, report `Saved/AgentLogs/tests/20260922-234414/` in lane eng2). A typical catch (6 stats, 2 modifiers) replicates in 272 bytes, over the 256-byte target in fish-system-rules.md (Q26). Breakdown from the test: empty slot 31 B; species/rarity/numbers 63 B; +180 B for the 6 stats (about 30 B each, because each stat's gameplay-tag name is sent as a string); +29 B for 2 modifier ids. Options for the owner (unreal-engineer, lead decides): turn on GameplayTags fast replication (tags go as a small net index, which saves roughly 150 B), write a NetSerialize for the stats array, or raise the target.
Round 2 (4f56d15), QA 2026-09-22 in lane qa1: 29 new tests, no new bugs. `-Filter Project` 86/86 pass, 0 warnings, report `C:/GameDev/VibeGame-lanes/qa1/Saved/AgentLogs/tests/20260922-234004/`.

### Observations from T-008 testing (no failing test; for the lead)
- T008-O1: MaxModifiers keeps the first hits in lexical row-name order, so which traits survive the cap depends on their names (Albino and Feisty beat Heavy/Giant/QA_*). The first run of Extensibility.NewRowsNeedNoCode lost a chance-1 modifier in 2.7% of catches with MaxModifiers 2. This is documented behavior, but renaming a modifier changes which ones survive. Consider an explicit priority column, or keeping the rarest (lowest RollChance) first.
- T008-O2: runtime data warnings repeat on every roll (one negative RollWeight gave 50,000 identical warnings over 50k rolls; an unknown allow-list id warns on every roll). If bad data ever ships, the log floods. Consider warning once per table/row/problem per session.
- T008-O3: an unknown ForcedModifierIds entry fails the whole roll (warning names the id). The spec is silent; the test accepts either "fail" or "drop the unknown id". Write the chosen rule into fish-system-rules.md.
- T008-O4: the validator doesn't check that XpMultiplier rises with Rank (it does for ValueMultiplier). Value.XpFormulaAndMonotone guards only the current starter data.
- T008-O5: unregistered tags written in JSON object form `{"TagName": ...}` or struct-text form `"(TagName=...)"` import with 0 problems (the struct-text form silently becomes an empty tag). FFishDataValidator catches both (Data.TagsRegistered), so keep the validator in the reimport path.

T003 history: T003-B1..B4 below are fixed.

Found 2026-09-22 by qa-engineer, report `Saved/AgentLogs/tests/20260922-221822/`. Owner: unreal-engineer (`Playtest/PlaytestNoteWriter.cpp`). All four FIXED 2026-09-22 in 1e4825b by unreal-engineer; `-Filter Project` 51/51 pass, report `Saved/AgentLogs/tests/20260922-222806/`.
- FIXED in 1e4825b: T003-B1 `Json.NonFiniteNumbersStayValid`: a NaN/Inf location, game time or FPS is written as `nan`/`inf`, so note.json is invalid JSON (Python `json`, PowerShell `ConvertFrom-Json` and jq reject it; the engine reader accepts it). Fix: NaN/Inf are written as `null`, and WriteNote reports the affected fields as one warning.
- FIXED in 1e4825b: T003-B2 `Failure.PixelCountMismatch`: the note folder is created before the pixels are validated, so a rejected write leaves an empty note folder. SubmitNote then drops the typed note. Fix: inputs are validated before anything is created. A bad or unwritable screenshot is dropped (`"screenshot": ""`, warning) and the note is still saved. Only a failed note.json returns false, and then the folders the call created are removed. On failure SubmitNote logs the text and the full note.json at Warning level.
- FIXED in 1e4825b: T003-B3 `Failure.EmptyRoot`: an empty root resolves against the process dir, and the note was written into `Engine/Binaries/Win64`. Fix: an empty or whitespace-only root is rejected with an error, and nothing is written.
- FIXED in 1e4825b: T003-B4 `Git.MalformedRepoIsUnknown`: non-hash HEAD/ref content (`this is not a commit`, `not-a-hash`, `abc123`) is returned as the commit id instead of "unknown". Fix: only 40 or 64 hex characters (trimmed) are accepted, otherwise "unknown". HEAD refs must be under `refs/`. A broken loose ref no longer falls back to a stale packed id.

## T-004 first-person movement (lane eng1)
Implementer tests: `Tests/LureMovementTest.cpp`, `Project.Movement.{Data,Stance,Camera,Net,Input,Setup}.*`, 14 tests (unreal-engineer).
Independent tests: `Tests/Movement/QAMovement*.cpp`, `Project.Movement.QA.*`, 150 tests (qa-engineer), written from the spec and the QA design (`Saved/AgentLogs/qa/eng1-T004-test-design.md` in main); decisions are in `docs/specs/movement-rules.md`.
Fixtures: tables are built from CSV text, never from the binary DT. Fixture A has values unlike the shipped CSV and the class defaults; Fixture B has other numbers, crouch jump on and prone TransitionTime 0. Test worlds are `FTestWorldWrapper` game worlds with static BlockAll boxes, stepped at 60 Hz. Boundaries: Clear(S) = 2 x CapsuleHalfHeight; "just below" = Clear - 1 cm must block; "just above" = Clear + 3 cm must pass (the 3 cm covers the 1.9-2.4 cm floor gap).

| Area | Tests (`Project.Movement.QA.` + ...) | Level | What they prove |
|---|---|---|---|
| DT_Movement data | Data.{CsvParsesCleanly, AllFourStancesPresent, NoDuplicateRowNamesRawScan, NumericCellsAreNumbers, SpeedsPositiveAndSane, SpeedOrdering, CapsuleHeightOrdering, EyeHeightOrdering, RadiusValid, EyeInsideCapsule, ProneFits60cmGap, CrouchDoesNotFit60cmGap, StandHeightHumanScale, TransitionTimeRange, NoiseMultiplierOrdering, JumpFlags, GapMinimumsReported} | D | Every row of the shipped CSV: 4 stances, number cells really numeric, speed/height/eye/noise ordering, radius <= half height, eye 10 cm under the capsule top, prone fits 60 cm, crouch doesn't, jump flags per A13/A14. Logs the level-design gap minimums: prone 54.4, crouch 112.4, stand 182.4 cm |
| Resolution and fallbacks | Fallback.{NoTableUsesBuiltInRows, SettingsPathMatchesEnvironment, BuiltInRowsPassValidation, MissingRowFallsBackPerStance, NonPositiveSpeedRejected, NonNumericCellHandled, HalfHeightBelowRadiusHandled, NegativeTransitionTimeHandled, WrongRowStructFallsBack, EmptyTableFallsBack, UnknownExtraRowIgnored, NonFiniteValuesRejected, HeightsOutOfOrderHandled} | U/I | A missing or invalid table or row falls back per stance with exactly one Warning (never an Error, A19). The fallback rows pass the same design rules. The settings path uses the imported asset once it exists (main) and falls back in lanes |
| Settings and game mode | Config.{SettingsPointToDTMovement, GlobalDefaultGameModeIsLure, GameModeSpawnsLureCharacter, BinaryTableMatchesCsv} | U/D | The config points at /Game/Data/DT_Movement and ALureGameMode. BinaryTableMatchesCsv compares the imported asset with the CSV row by row (it skips while not imported) |
| Character setup | Character.{CameraIsFirstPerson, RotationSetupIsFirstPerson, StandEyeHeightFromData, ArmsOwnerOnlyAndAttachedToCamera, ArmsSoftRefPath, MissingArmsAssetIsNullSafe, ArmsLoadWhenAssetPresent, PlaceholderBodyFollowsStance, NonLocalPossessionIsSafe, TwoCharactersIndependent, DestroyMidTransitionIsSafe, ReplicationFlags} | I | FP camera (no boom; eye inside the prone radius); arms owner-only, on the camera, no collision or shadow; missing arm assets are null-safe; the olive body is seen by others only and matches the capsule per stance (A18); remote/AI possession is safe; characters don't share state |
| Speeds | Speed.{Stand,Sprint,Crouch,Prone}MaxSpeedFromData, ReachedSpeedMatchesMax, SprintReleaseReturnsToWalk, SwappingTableChangesSpeeds, SprintHeldWhileStandingStill, NoiseMultiplierPerStance | I | Max and reached speed per state come from the table (Fixture A and B). Sprint held while standing still is not sprinting (A2). Noise multiplier per state (T-014 prep) |
| Stance matrix | Stance.{StandToSprint, StandToCrouch, StandToProne, SprintToStand, SprintToCrouchCancelsSprint, SprintToProneCancelsSprint, CrouchToStandRestoresTableHeight, CrouchPlusSprintStandsAndSprints, CrouchToProne, ProneToStand, PronePlusSprintIgnored, ProneToCrouch, RequestCurrentStanceIsIdempotent, SameFrameConflictingRequestsDeterministic, RapidToggleSpam, ProneWhileFallingRejected, CrouchWalksOffLedge, ProneWalksOffLedge, ToggleKeysFollowDecisions} | I | Every allowed transition: stance, capsule and speed follow the row; no penetration; feet planted; stays on the ground. Standing height is the table's, not the class default. A1 sprint rules, A3 sprint resumes, A5 last request wins, A6 no prone in the air, A16 crouch walks off ledges. 120 seeded random inputs keep every invariant |
| Headroom | Headroom.{CrouchToStandBlockedJustBelow, CrouchToStandAllowedJustAbove, ProneToStandBlockedJustBelow, ProneToCrouchBlockedJustBelow, ProneToCrouchAllowedJustAbove, ProneToStandAllowedJustAbove, Under60cmSlabCannotRise, QueuedStandCompletesWhenCeilingRemoved, OtherStanceInputCancelsQueuedStand, WiderStandCapsuleNextToWall, StandUpInACorner, NudgeLimitDecidesStandUp, SprintFromCrouchUnderCeilingBlocked} | I | Stand and crouch boundaries at -1/+3 cm. No partial rise. A blocked stand-up keeps the camera still, stays queued and completes when there's room; other input cancels it (A4). Standing up next to walls or in a corner never penetrates; the push stays within MaxStanceNudge, and beyond it you stay prone |
| 60 cm gap | Gap.{ProneSweepsUnder60cm, ProneSweepsUnder61cm, ProneAt59cmMatchesData, ProneBlockedBelowItsHeight, ProneWalksThrough60cm, CrouchSweepBlockedBy60cm, CrouchWalkStopsAt60cm, StandBlockedCrouchPassesCrouchGap, StandRequestInsideGapThenExit, FixtureAProneFits60cm} | I | Prone passes 59/60/61 cm (it needs 54.4) and walks through without falling or penetrating; crouch stops at the edge; a stand request inside the gap waits and completes after crawling out |
| Jump | Jump.{StandCanJump, SprintCanJump, CrouchJumpFollowsData, ShippedCrouchCanJump, ProneCannotJump, ProneCannotJumpEvenIfDataSaysSo, PressWhileProneKeepsStance, NoProneMidAir, JumpAndProneSameFrame, StanceKeptThroughLanding} | I | Stand and sprint jump (no speed boost). Crouch jump follows the data (on in the shipped data). Prone never jumps, even if the data says so (A13); a press keeps the stance. Never prone in the air |
| Eye and camera | Eye.{SettledEyePerStance, MidTransitionIsBetween, ReachesTargetInTime, NoOvershoot, NoPopWhenCapsuleResizes, InterruptReversesSmoothly, ZeroTransitionTimeSnaps, FrameRateIndependent, Deterministic, SprintDoesNotMoveCamera, HitchClamped, PawnViewLocationMatchesCamera, NearClipNeverInsideCeiling, CameraSteadyWhileWalking} | I | Eye from the feet per stance; eased over the target row's time (A8), no overshoot, no pop at resize (<= 0.4 x dEye per frame), same curve at 30 and 120 fps, T = 0 snaps. The AI eye point is the camera (T-016). Going prone at the gap's mouth never puts the near-clip sphere inside the slab (A9). No camera bob |
| Arms bob math | ArmsBob.{StepRateFormula, StandingStillHasNoBob, BoundedByAmplitudes, FullSpeedReachesFullAmplitude, AirborneFadesOut, HoldingRodIsSteadier, LookSwayLagsAndIsClamped, Deterministic, SafeWithOddDeltaTimes} | U | FLureArmsBob::Step against the formula in SK_FPArms.anim.md |
| Prediction and replication | Net.{SprintUsesOneCustomFlagBit, ProneUsesOneCustomFlagBit, CompressedFlagsRoundTrip, MovesWithDifferentFlagsDoNotCombine, ClearResetsCustomFlags, ServerIgnoresSprintWhileProne, ServerEnforcesHeadroom, StanceStateIsReplicated, SimulatedProxyAppliesStanceCapsule} | U/I | One custom bit each; the flags round-trip to a server CMC (crouch bit intact); moves with different wishes never combine; Clear resets the bits. The server caps prone speed and enforces headroom. bIsProne is COND_SimulatedOnly with an OnRep; proxy capsules follow crouch and prone in either notify order |
| Input | Input.{AllSixActionsResolveByName, ValueTypes, UnknownNameReturnsNull, LookupIsStableAndMapped, ActionsSurviveGarbageCollection, KeyboardAndGamepadBindings, NoKeyBoundToTwoActions, PlaytestKeyF8IsFree, ByNameLookupCallableFromPython, PossessionBindsAllActions, ExecutingBindingsDrivesStances} | U/I | Six actions by name are the same objects the IMC maps and the character binds; they survive GC; KB/M and gamepad keys exist with no conflicts and F8 stays free; the lookup is Python-callable. Local possession binds every action, and firing the bindings drives crouch/prone toggles, sprint hold, move and jump |

Latest runs (lane eng1): see the QA report `Saved/AgentLogs/qa/*-eng1-T004.md` in main for the run directories.

### T-004 findings and notes (qa-engineer, 2026-09-22)
- T004-Q1 (minor, open, decision for the lead): after crawling off a ledge the character stays prone for up to 3 frames (50 ms) in the air. The stand-up is blocked while the wider stand capsule still overlaps the ledge corner; it stands up in the air once clear, never penetrates, and goes prone again on landing. `Stance.ProneWalksOffLedge` asserts at most 0.1 s. If "never prone in the air" must be strict, UnProne in the air needs another candidate position (e.g. away from the ledge).
- T004-Q2 (data pipeline, applies to EVERY DataTable): the engine's CSV import turns non-numeric text in a number cell (`fast`, `0.2x`) into 0 and reports NO import problem (`Fallback.NonNumericCellHandled` logs "import problems: 0"). For columns where 0 is valid, a typo becomes a silent 0. DT_Movement is guarded by `Data.NumericCellsAreNumbers`; every other `data/tables/DT_*.csv` (fish, rarity, modifier in T-008) needs the same raw-text check.
- T004-Q3 (info, gap): whether two IDENTICAL hand-built saved moves combine is not deterministic in a headless test world (walk/sprint observed as 0/0 in one run and 1/0 in the next; the moves come from AllocateNewMove outside the real client move pipeline). So bandwidth combining isn't asserted, only reported. `Net.MovesWithDifferentFlagsDoNotCombine` asserts the part that matters for correctness: moves with different sprint/prone/walk wishes never combine. If the playtester sees higher bandwidth or odd corrections while sprinting in 2-player PIE with net emulation, look here first.
- Harness notes for test authors: call `UClass::SetUpRuntimeReplicationData()` before `GetLifetimeReplicatedProps` on a CDO (otherwise RepIndex is unassigned and the engine's duplicate check crashes the whole run). Measure the "camera doesn't move" baseline only after the eye blend has finished (about 40 frames). A capsule stopped by a low edge touches it with its rounded top, so its axis stops inside one radius of the edge.
- Known unrelated failure in lanes: `Project.Playtest.GitCommit` (T003-B5, worktree commondir; being fixed in main).

### T-004 gaps (not covered by automation)
- Real network prediction under latency, server corrections, what another player sees, late join: the playtester's 2-player PIE with net emulation (checklist `Saved/AgentLogs/qa/eng1-T004-playtester-checklist.md` in main).
- Feel (speeds, acceleration, TransitionTime, FOV, mouse smoothing), arms framing, clipping, bob and stance-dip visuals: playtester and designer. The arms assets (SK_FPArms, ABP, StanceDip) aren't imported in the lane, so `Character.ArmsLoadWhenAssetPresent` and `Config.BinaryTableMatchesCsv` skip until run in main after the import.
- The real key -> action path through a LocalPlayer (injection): playtester / T-025. Action -> handler is covered by `Input.ExecutingBindingsDrivesStances`.
- The map scenario (L_Dev_Movement gaps and ceilings, T-005 crawl cave): playtester, and later the T-022 bot run.
- Prone fishing (rod tuck while crawling): T-006/T-007, not in T-004 code yet.

## T-026 surface swimming + T-004 playtest fixes (lane eng2)
Implementer tests: `Tests/Movement/LureSwimTest.cpp` (`Project.Movement.Swim.*`, `Project.Movement.Climb.NoClimbRuleLandsOnSlopes`), `LureSwimNetTest.cpp` (`Project.Movement.Swim.Net.*`, 7), `LureMovementPlaytestFixTest.cpp` (`Project.Movement.{Climb,Stance,Camera}.*` B1-B3).
Independent tests: `Tests/Movement/QASwimTest.cpp` (qa-engineer, 21 tests), from docs/specs/swimming.md, movement-rules.md and review gaps T0/T2-T5. Worlds: transient game worlds, water surface z = 0, seabed -600, 60 Hz; the table is data/tables/DT_Movement.csv with rows edited in memory.

| Area | Tests (`Project.Movement.QA.` + ...) | Level | What they prove |
|---|---|---|---|
| Entering the water | Swim.Enter.{FromEveryStanceOffAnEdge, JumpFromTheEdge, FallFromHeightSettlesWithOneEvent} | I | Walk/sprint/crouch/prone off 30 and 100 cm edges, a stand or crouch jump at the edge, a 10 m fall into deep and 150 cm water with settle times 0.05/0.8/3 s: swimming, standing, Stand capsule, no wish kept, SwimSprint while sprinting, float depth, eye height, no penetration; after the plunge never out of the water and exactly one "in" AFTER settling (review T3). Jump back out works at 30 cm, not at 100 cm |
| Wading vs swimming | Swim.Depth.{WadingVsSwimmingThreshold, CrouchOrProneWhileWadingNoSwimFlicker} | I | 60/88 cm deep = walking, 96/150 = swimming, no events while moving about. Crouch at 60 / prone at 30 cm: no swim-event blip (FAILS, T026-B2) |
| Two volumes | Swim.Volumes.OverlappingAndTouchingKeepSwimming | I | Overlapping (400 cm) and touching boxes: never out of the water, surface found, height steady, no events |
| Ladders | Swim.Ladder.{GrabZoneBoundaries, ClimbHeightBoundaries, EnterClimbTopAndBackIn} | U/I | Zone +-1 cm at reach/width/height for 3 yaws, climb direction; dock 300 = ladder 300 climbs, 301 doesn't, 150 with ladder 100 doesn't, a ladder below 60 keeps the row's rule, no room to stand = refused; outside the zone Jump does nothing; in-water for the whole climb, crouch refused mid-climb, one "out" on top, climb time ~ path/speed, walking off the top = "in", leaving the zone restores the row's limit |
| Leaving | Swim.Leave.{StancesAndSprintWorkAgainOnLand, StepOntoSubmergedShelfNoLaunch} | I | Sprint/crouch/prone work on land after a climb out. Stepping from the water onto shelf tops -58..-90 cm: no launch, ends walking on the shelf (FAILS, T026-B3) |
| Teleport / respawn | Swim.Teleport.{BetweenWaterAndLand, MidClimbEndsTheClimb}, Climb.TeleportMidPullUpEndsIt | I | Water->land "out", land->water "in", water->water nothing. A teleport mid ClimbOut / LedgeClimb ends the climb at the destination (FAIL, T026-B1) |
| Swim events | Swim.Events.OncePerRealChangeOverATrip | I | Fall in, sprint, Jump in open water, stance requests, a 150 cm wall, climb out, walk off, beach walk-out: exactly "in,out,in,out", "out" only once standing |
| Diving path | Swim.DiveRow.SurfaceFloatDepthZeroIsFreeSwimming | I | Review T2: depth 0 is accepted (no fallback), no surface rule, sinks and is not pulled back up, row speed, no prone |
| No shelf dead band | Swim.Leave.NoShelfDeadBand | I | Regression for a60e4a5 (was Gap.SubmergedShelfDeadBandDocumented): vertical shelf tops -91..-15 cm are all stepped onto or climbed with Jump; none stuck; steps not launched (feet < top+20, vz <= 300), climbs peak < top+60 |
| Arms | Swim.Arms.StrokeClipPlaysInDefaultSlot | I | Review T4: with a stroke clip (A_FPArms_Idle stands in) the montage plays, ABP_FPArms blends DefaultSlot > 0.9, lowering off, stops on land |
| Climb rule B1 | Climb.{EdgeLandingRefusedWhereTheEngineAccepts, NeverPerchedBelowALedgeTop} | I | Review T0: 40 edge contacts the engine's IsValidLandingSpot accepts are all refused; flat tops accepted. Frame by frame never walking perched on a 100 cm block's edge (fall without jump, jump released at apex, stand/crouch/sprint jumps; stand and crouch reach the top through the pull-up) |
| Arms pull-back | Camera.ArmsPullBackFollowsData | I | Review T5: Stand 4 / Prone 9 in the data put the arms at X -4 / -9 |

Implementer-test fixes by QA: `Project.Movement.Camera.ProneArmsPulledBackFromWalls` expected +Stand.ArmsPullBack (wrong sign, passed only while it is 0), now -Stand.ArmsPullBack (review T5). Fixture edits reviewed (sound): `QA.Data.AllFourStancesPresent` (6 rows) now also checks one row per ELureMovementState; `QA.Fallback.UnknownExtraRowIgnored` ("Swimm") now also checks that Swim comes from its own row.

### Open bugs (T-026, found 2026-09-23 by qa-engineer, report `Saved/AgentLogs/qa/20260923-*-T026.md` in main)
- T026-B1 (major) `Swim.Teleport.MidClimbEndsTheClimb`, `Climb.TeleportMidPullUpEndsIt`: TeleportTo during ClimbOut/LedgeClimb keeps the plan, so PhysClimb drags the character from the destination back toward the old edge at climb speed (423 / 503 cm in 1.5 s), still in MOVE_Custom and IsSwimming true on land.
- T026-B2 (minor, needs a rule) `Swim.Depth.CrouchOrProneWhileWadingNoSwimFlicker`: crouch in 60 cm or prone in 30 cm water puts the capsule center under the surface: one frame Swimming, the wish is cleared, back to Stand; OnSwimStateChanged fires "in,out" (T-006 would cancel fishing in the reef shallows).
- T026-B3 (major) `Swim.Leave.StepOntoSubmergedShelfNoLaunch`: stepping from the water onto a vertical shelf top at -58/-60/-65/-70 cm throws the swimmer up (feet to +591/+456/+182/-29 cm, up to 1148 cm/s); -75 cm can't be stepped onto at all; -80..-90 fine. L_PalmKey's reef flat is at -60.
- Open (review D2, no test yet): with SurfaceFloatDepth 0 the climb out works from any depth.

### T-026 gaps
- Real 2-player PIE (latency, corrections, proxies): playtester, checklist in the netfix report.
- Swim stroke: the real A_FPArms_SwimStroke clip, and a clip that fails to play (null montage retried every tick, review T4 case 2).
- Overlapping volumes with different surface heights or priorities; ladders on pitched docks.

### T-026 x T-006 merge (lane eng2, implementer tests in `Tests/Movement/LureSwimFishingTest.cpp`)
- `Project.Fishing.ArmsPose.GraphPinOrderMatchesEnum` (I, qa): locks ABP_FPArms Blend Poses pin order. For Idle/HoldRod/ProneHold/ProneTuck (rod off/on, prone still, prone crawling), after 1.5 s hand_r_rod (component space) is closest to that pose's own clip (clips sampled on a reference mesh). Skips with a log line if the ABP or clips are not loadable.
- Lead decision (2026-09-23): DT_Movement Swim rows keep CanFish=True. Swimming blocks fishing in code (IsSwimming); CanFish describes the land stances only. No change to MovementRodRules.
- `Project.Fishing.Rules.ClimbingIsBusy` (U): MOVE_Custom climbs block a cast and reel a line in (reason Climbing); the climb out reports Swimming; a plain jump keeps the line.
- `Project.Fishing.Swim.ClimbOutBlocksFishingAndACastRightAfterStays` (I): swimming = no rod, Idle arms, cast refused; the whole climb out refuses casts; on the dock a cast goes out at once and survives a late OnSwimStateChanged pair; rod and HoldRod back.
- `Project.Fishing.Climb.LedgePullUpReelsTheLineIn` (I): a line out survives the jump, the LedgeClimb pull-up reels it in (Climbing) and refuses a new cast.
- `Project.Movement.Swim.Depth.StanceHintOnHud` (I): a crouch refused while wading shows "Too deep to crouch here." in `ALureHUD::GetStatusText`, gone after StanceHintDuration.
- For QA: `Project.Fishing.QA.Data.MovementRodRules` still expects "only Sprint can't fish", so the Swim rows keep `CanFish = True` (swimming blocks fishing in code). If QA prefers `CanFish = False` on the Swim rows, flip that assertion and the CSV/fallback rows together.
## T-010 cooler, selling, money, XP and levels (lane eng5)
Implementer tests: `Tests/Progression/Progression{Component,Data,Interaction,Rules}Test.cpp`, `Project.Progression.{Cooler,Level,Landed,Money,Authority,Caught,Save,Data,Sell,Interact,Net}.*`, 25 tests (unreal-engineer).
Independent tests: `Tests/Progression/QAProgression*.cpp` (+ `QAProgressionTestUtils.*`, `QAProgressionListener.h`), `Project.Progression.QA.*`, 42 tests (qa-engineer), from `docs/specs/progression-rules.md` and the T-010 acceptance.
Fixtures: tables from CSV text only (the data/tables sources or QA fixtures). QA fixture: levels XpToNext 50/80/120/200/0 (starts 0, 50, 130, 250, 450, cap 5); coolers Basic 4 / Mega 10 / One 1; markets Default 1.0 / Fancy 1.25 / Stingy 0.5 / Triple 3.0. Real catches come from the fish roll pipeline (`FishQA::LoadReal`).

| Area | Tests (`Project.Progression.QA.` + ...) | Level | What they prove |
|---|---|---|---|
| Data (shipped CSVs, QA's own rules, not the implementer's validator) | Data.{SourcesImportCleanly, RawNoDuplicateOrEmptyRowNames, RawColumnsMatchRowStructs, RawNumericCellsAreNumbers, PlayerLevel.LevelsContiguousFromOne, PlayerLevel.XpToNextPositiveBelowCapZeroAtCap, PlayerLevel.CurveNeverGetsEasierAndFits, Cooler.RowsSaneAndDefaultExists, Market.RowsSaneAndDefaultAndDockExist, NewRowsNeedNoCode} | D | All three tables import clean; raw scans catch duplicate names, unknown columns and text in number cells (the engine reads those as 0); levels 1..N, XpToNext > 0 below the cap and 0 at it, never easier, cap XP far below int32; cooler slots 1..100, the settings' default rows exist; market multipliers finite in (0, 10]; the PalmKeyDock row exists; a new level, cooler and buyer added as rows work with no code |
| Pure rules | Rules.{Price.RoundHalfUpNotBankers, Price.FormulaMinimumAndMonotone, Price.DependsOnlyOnRolledValue, Total.SumOfSinglesAndOrderFree, SaturatingAddExtremes, Curve.ShippedStartsMatchSummedCsv, Curve.MonotoneAndBounded} | U | Price = max(1, floor(Value x m + 0.5)) (2.5 -> 3), >= 1, monotone in Value over 1..3000 at 16 multipliers; only Value matters (species/rarity/weight/modifiers are inside it); totals = sum of singles in any order (300 seeded catches); saturating add at MIN/MAX; curve starts equal independently summed CSV, level monotone and bounded up to int64 extremes |
| Cooler | Cooler.{ShippedBasicFullReleasesNinthKeepsXp, KeepsRealCatchRecordsExactlyInOrder} | I | Shipped Basic cooler: N fish in slots 0..N-1, the next is released with its XP kept and no stored fish changed; a freed slot is reused; real rolled records are stored bit-exact in order |
| Selling | Sell.{EmptyCoolerPaysNothingAndFiresNoEvents, RealCatchAtPalmKeyDockPaysRolledValue, UnknownMarketIdPaysTimesOne, InvalidMarketRowFallsBackToTimesOne, MoneySaturatesAtMaxOnSale} | I | Empty cooler: 0/0 and no events on every sell path; roll -> land -> sell at PalmKeyDock pays the spec formula, the quote matches, no XP/level from selling; unknown MarketId and 0/negative multiplier rows pay x1; money saturates at MAX_int32 |
| Money and levels | Money.NegativeAndExtremeAmountsRefused; Level.{OneLandingCrossesSeveralLevels, ExactThresholdsJustUnderAndOver, XpSaturatesAtMaxInt, DifficultyWithoutProgressionIsLevelOne} | I | MIN/negative/over-balance amounts refused; one landing 1 -> 5 on the shipped curve = one OnLevelUp(1, 5); thresholds at -1/0 XP; XP saturates at MAX with the level at the cap; the level-gap multiplier falls back to level 1 without progression |
| Authority and interaction | Authority.{ClientCannotSellAnyWay, ClientOnRepsOnlyNotify}; Interact.{PromptRangeIsTheRadius, ServerAcceptsWithinSlackRefusesBeyond, BadTargetsAndSlotsRefused, PawnWithoutPlayerStateSellsNothing}; Net.TwoPlayersSellOnlyTheirOwnCooler | I | Owning and other clients can't sell, land or grant through any path (SellAll/SellOne/Interact/TryInteract/RequestInteract/key); OnReps notify but never mutate; prompt range = radius (+-1 cm, 3D); server accepts radius + 150 cm - 1 and refuses + 1; null, non-interactable, out-of-range slots refused; two players at one dock: each sells only their own cooler |
| Save and travel | Save.{RealCatchRoundTripsExactly, ApplyReplacesAndIsIdempotent, DefaultSaveLoadsAsNewPlayer, HostileValuesAreClamped, UnknownCoolerRowKeepsFish, SeamlessTravelCopiesProgress} | U/I | Real catches survive a SaveGame archive + apply bit-exact; apply replaces (not merges), twice = once, never a level-up; default struct = new player; MIN/0/MAX values clamp; an unknown saved cooler row keeps every valid fish in order (broken record dropped); CopyProperties and OverrideWith carry everything |

### T-010 findings (qa-engineer, 2026-09-23)
- T010-O1 (hardening, not reachable by players): `ULureInteractionComponent::TryInteract` / `ALureSellPoint::SellAll` accept a destroyed (pending-kill) sell point passed directly and still sell. The player path is safe (the subsystem unregisters it, and a server RPC resolves a destroyed actor to null), so no failing test; suggested one-line fix: `IsValid(Target)` in TryInteract and the sell functions.
- T010-O2: RESOLVED in 40c547b. An unknown CoolerId now switches to DefaultCoolerId, with a warning (progression-rules.md); test Project.Progression.Save.UnknownCoolerUsesDefaultRow.
- T010-O3: RESOLVED in 40c547b. The fallback size lives only in LureProgressionSettings.h and is used only when DT_Cooler or its default row is missing.

### T-010 gaps (not covered by automation)
- Real replication over a net driver (owner-only fish list delivery, the Interact RPC from a real client, a joining client not getting OnLevelUp for its initial level): playtester 2-player PIE.
- The sell point placed at the L_PalmKey dock (editor-operator) and the placeholder HUD text: playtester scenario.
- The binary /Game/Data assets vs the CSVs (after import in main): add an AssetMatchesSource-style check like the fish tables have.

## T-006 casting, bobber, bite, hook (lane eng1)
Implementer tests: `Tests/FishingTest.cpp`, `Project.Fishing.*` (19 tests, unreal-engineer).
Independent tests: `Tests/Fishing/QAFishing*.cpp` + `QAFishingTestUtils.{h,cpp}`, `Project.Fishing.QA.*`, 90 tests (qa-engineer), black-box from docs/specs/fishing-rules.md and GAME_DESIGN. All code is in `namespace QAFishing` (unity-build safe). Worlds are transient game worlds stepped at a fixed dt with fixed seeds; profiles are built from the CSV or the shipped row with explicit overrides, so the tests never depend on the placeholder AutoLandDelay (T-007 sets it to 0).

| Area (count) | Tests (`Project.Fishing.QA.` + ...) | Level | What they prove |
|---|---|---|---|
| Data (9) | Data.{EnumCellsImportAsNamed, FishingBuiltInProfileEqualsCsv, FishingCsvCellsAreTyped, FishingRowsMeetDesignLimits, FishingValidateRejectsBadRows, MovementBuiltInRowsEqualCsv, MovementCsvCellsAreTyped, MovementRodRules, MovementValidateRejectsBadRodColumns} | D | DT_Fishing and the DT_Movement rod columns: every cell typed (enums exact names, never _MAX), design limits (hook window, bobber scale 3x, dip depth, waits fit the ~30 s loop; AutoLandDelay read from the table, 0 allowed for T-007), Validate() rejects bad rows, the built-in rows equal the CSV. |
| Cast (15) | Cast.{AimYawSetsDirection, ArcShape, ChargeFromHoldTime, DistanceClampedForAnyCharge, DistanceEndpointsFromData, DistanceMonotoneForAnyExponent, FlightTimeClamped, InvalidProfileRowFallsBack, LandingFollowsTheTable, MissingOrBadTableUsesBuiltInProfile, ProfileRowSelection, RefusedCastKeepsTheLineIdle, SecondCastWhileLineOutIsRefused, ServerClampsRequestedCharge, SettingsPathMatchesEnvironment} | U/I | Charge from hold time; distance and flight time from the table (endpoints, monotone for any exponent, clamped for any charge); the server clamps the requested charge; aim yaw sets the direction; profile row selection, bad/missing table falls back to the built-in profile; a second cast while the line is out is refused. |
| State (4) | State.{CastButtonPerState, CastingLastsTheFlightTime, FullSequenceInOrder, ReleasedChargeIsTheCastCharge} | U/I | Full sequence Idle -> Charging -> Casting -> Waiting -> Bite -> Hooked -> result in order; the cast button per state; Casting lasts the flight time; the released charge is the cast charge. |
| Hook (16) | Hook.{BiteSequenceDeterministicWithSeed, BiteWaitWithinDataRange, EarlyPressDuringNibbleFollowsRule, HookedAndLandedFishIsTheBite, JustAfterTheWindowMisses, JustInsideTheWindowHooks, LatePressIsAMiss, MissLosesTheFishForGood, NibblesWithinDataRange, PressWhileHookedChangesNothing, PressWithNothingToHookReelsIn, RemoteGraceOnlyForRemotePlayers, ServerNeverForcesTheRoll, SpookDelaysBiteAndItsNibbles, WindowComesFromTheTable, WindowEdgesPure} | U/I | Hook window edges (pure and in the world, just inside/just after), remote grace only for remote players, early press per EarlyHook rule, spook, miss/late press, the hooked and landed fish is the bite (field by field), nibble and bite-wait ranges, determinism with a fixed seed, the server never forces the roll. |
| Spot (16) | Spot.{BobberOnLandInsideSpotNoBite, ContextFromSpotAndEnvironment, DeepestSpotWins, LayoutMarkersParseCleanly, MarkerDataReachesTheRoll, MarkersAreReadLive, NoFitHintAfterHintDelay, NoFitSpotBitesWhenTimeFits, NoFitSpotShowsNoNibbles, NoSpotNoBiteEver, OffSpotHabitatSettingEnablesBites, ParseMalformedTagsNeverBreak, ParseRadiusRules, ParseSoftProblemsKeepTheSpot, RadiusBoundaryIs2D, TaggedWaterSurface} | U/I | Spot tag parsing (malformed, radius, soft problems), layout markers, 2D radius edge, deepest spot wins, context reaches the roll, no spot / no fit / land never bite, live markers, the off-spot habitat setting, tagged water. |
| Interrupt (11) | Interrupt.{JumpKeepsTheLine, PawnDestroyedAtEveryStage, ProneCrawlEndsEveryStage, ProneStillKeepsEveryStage, RodPutAwayEndsEveryStage, SpotLostWhileFishing, SprintEndsEveryStage, SprintHeldStandingStillKeepsTheLine, StanceChangesKeepTheLine, SwimEndsEveryStage, TooFarFromTheBobber} | U/I | Every stage x sprint/prone crawl/swim/rod put away/pawn destroyed/spot lost/too far ends the line; stance changes, prone still, jump and sprint held standing still keep it. |
| Pose (9) | Pose.{ArmsCounterPitchFormula, CharacterPoseFollowsStanceAndMotion, ClearanceTraceBoundaries, ColumnsDriveThePoseAndTheLineRules, PitchFollowUpEases, ShippedRowsPerStanceAndMotion, SwitchHysteresisEdges, WallAheadTucksUnlessFishing, WallTuckRules} | U/I | Rod pose switch rule and hysteresis edges, wall tuck (unless fishing), counter-pitch formula, clearance trace boundaries, data columns drive the pose. |
| Net (7) | Net.{BiteStaysSecretUntilHooked, ClientCopyCannotDecide, ClientFollowsServerAtEveryStage, NetStateWireRoundTrip, OnlyChargeAndYawCrossTheWire, ReplicatedToEveryone, ServerRpcsClampHostileRequests} | U/I | Only charge and yaw cross the wire; replication conditions; NetState FRepLayout round trip; a client copy cannot decide; the bite stays secret until hooked; hostile RPC values are clamped. |
| Net2P (1) | Net2P.{ServerDecidesClientsFollow} | I | A real in-process dedicated server with 2 clients: the server decides, both clients follow. |
| Input (2) | Input.{BoundActionsDriveFishing, KeysFromSettings} | U/I | Keys come from settings; bound actions drive fishing. |

Latest runs (lane eng1, 2026-09-23): `-Filter Project.Fishing.QA` 86/90 pass (`Saved/AgentLogs/tests/20260923-103804`); `-Filter Project` 494/501 pass, 0 not run (`Saved/AgentLogs/tests/20260923-105427`). The 4 QA failures are the open bugs below; the other 3 are lane-environment failures (DT_Movement not re-imported: `Movement.QA.Config.BinaryTableMatchesCsv`, `Movement.QA.Fallback.SettingsPathMatchesEnvironment`; `Fish.QA.Data.AssetMatchesSource`, fixed on main in 2ae0c5a, lane not rebased).

### T-006 open bugs (failing tests; each test is the regression test for its bug; owner unreal-engineer, `Fishing/LureFishingComponent.cpp`)
- T006-B1 (medium) `Spot.NoFitSpotShowsNoNibbles`, `Spot.NoFitHintAfterHintDelay`: at a spot where no species fits (reef at noon), the bobber still shows nibbles (false tells: 2 shown, expected 0), and the "Nothing is biting here" hint only comes at the first failed bite check (up to BiteWaitMax 10 s), not at NoBiteHintDelay (8 s). The spec says it must behave like "no spot". With the clock fixed at 12:00, reef_flats and jetty_end are always in this state in playtests.
- T006-B2 (low) `Hook.SpookDelaysBiteAndItsNibbles`: EarlyHook = Spook uses `NextBiteTime = max(NextBiteTime, Now + SpookDelay)`, so a bite due in 5 s is not pushed back by SpookDelay 3 s (still +5.00 s, expected +8.00 s), while the pending nibbles are shifted +3 s, land after the bite and are lost (0 of 2 came).
- T006-B3 (low) `Net.ServerRpcsClampHostileRequests`: `ServerCast(1, 1e30)` passes the finite but huge yaw straight to `FRotator(0, Yaw, 0)` in `AuthorityCast` (only NaN/Inf are replaced), and the engine logs `LogCore: FRotator::Vector() provided with unreasonably large input values (... Y=1e30 ...)`. Fix: normalize the yaw (`FRotator::NormalizeAxis`) or reject out-of-range values on the server.

### T-006 observations (no failing test; for the lead)
- T006-O1: a late press just after the window reels the line in (EarlyHook ReelIn), so MissEndsCast False rarely keeps the bobber in practice.
- T006-O2: a press during the cast flight is ignored, while the spec's Controls say "a press reels in early".
- T006-O3: Validate() doesn't check MaxLineLength > MaxCastDistance (the shipped data satisfies it; Data.FishingRowsMeetDesignLimits guards it).
- T006-O4: Point Ledge and Gull Key Drop (Habitat.DeepDrop) have no species, so they never bite.
- T006-O5: the nibble jitter can merge two nibbles when NibblesMax >= 3.
- T006-O6: spot tags `Luck=.` and `Luck=-` parse silently as 0 (no soft problem reported).

### T-006 gaps (not covered by automation)
- Listen-server host path in a real 2-player session (Net2P uses a dedicated server; the host case is covered only by `Hook.RemoteGraceOnlyForRemotePlayers`); real latency and packet loss: playtester 2-player PIE with net emulation.
- Visuals and feel: bobber readability, bite dip and rumble, line rendering and sag, rod poses and arms framing: playtester and designer.
- The real key -> action path through a LocalPlayer: playtester / T-025.
- The reel fight after Hooked: T-007.

## T-007 reel fight, line tension and gear (lane eng3)
Implementer tests: `Tests/FishFightTest.cpp`, `Project.Fishing.Fight.*`, 10 tests (unreal-engineer).
Independent tests: `Tests/FishFight/FightQA*.cpp` + `FightQATestUtils.h`, `Project.Fishing.Fight.QA.*`, 45 tests (qa-engineer, 2026-09-23), written from
`docs/specs/reel-fight-rules.md` and the formula comment in `Fishing/FishFight.h`. Fixtures: the text sources in `data/tables/` (never the binary
assets), in-memory fixture rows, and "timing" tuning with instant tension easing so time over/under a threshold is a whole number of steps.
Clients are copies of the component whose owner has a non-authority role; replication goes through FRepLayout (bits written from the server
object, read into the copy, RepNotifies called on change), not a property copy.

| Area | Tests (`Project.Fishing.Fight.QA.` + ...) | Level | What they prove |
|---|---|---|---|
| Tension math at the edges | Math.{ZeroDragFreeSpool, MaxDragLocksTheLine, DragAtLineStrengthNeverSnaps, ZeroRodPowerIsSafe, FormulaSwitchPoints, DegenerateFishIsSafe} | U | Drag 0 = free spool (no tension, full swim speed, the hook is thrown); a locked drag (1e6, FLT_MAX) never gives line and snaps a weaker line; drag = line strength never snaps; rod power 0 gains nothing, stays finite, and such DT_Gear rows fall back; every switch point of steps 3-4; NaN/Inf/negative/huge stats stay finite through a whole fight |
| Snap / slack boundaries | Snap.{ExactlyAtGraceHolds, TimerResetsWhenTensionDrops, ZeroGraceSnapsOnFirstStepOver}; Slack.{ExactlyAtGraceHolds, TautStepResetsTimer, AtThresholdIsNotSlack} | U | "Longer than" the grace: exactly N steps holds, N+1 ends it (shipped data + fixtures; every DT_Gear hook); one step under/taut resets the timer; grace 0; tension exactly at the slack threshold is not slack |
| Spool | Spool.{FishTakesWholeSpool, BoundaryIsMoreThanTheSpool} | U | A fish let run (or out-pulling the rod 2x while reeled) takes the whole spool on the first step past it; exactly the spool length holds; a hook beyond the spool ends at once |
| Outcomes | Outcome.{FirstThatAppliesWins, LandedAtExactlyLandDistance, FrozenAfterTheEnd} | U | Landed > Spooled > Snapped > ThrewHook on the same step; LandDistance inclusive; Step/Advance do nothing after the end |
| Whole step vs the spec | Sim.StepMatchesSpecFormulas | U | An independent transcription of FishFight.h steps 2-6 matches every step of 172 fights (random fish, gear, tuning within the validated ranges, shipped/built-in/random patterns, random reel input, real rolled fish): 137k steps, all four outcomes reached |
| Moves, determinism | Sim.{MovePickFollowsWeights, MoveDurationsAndExhaustion, FrameRateIndependent, HitchIsBounded, IgnoresGlobalRandom} | U | Pick shares = max(0, W + A x AW) / sum; durations in [Min, Max] (rest x RestScale) and both ends reached; exhausted for good; 30/60/144 fps and random frame times give the identical fight; hitches capped at 30 steps; the global RNG changes nothing; FightSeed per record |
| Acceptance, one stat at a time | Gear.EachStatChangesTheOutcome | U | Line strength alone (Mono snaps / Braid lands the reference Coral Snapper), rod power alone (spooled / landed), hook security alone (Shrimp throws / Squid lands) |
| Data validation | Data.{GearEveryRow, GearBadRowsRejected, PatternEveryRow, PatternBadRowsRejected, PatternNeedsAPlainWeight, FishFightEveryRow, FishFightBadRowsRejected, FishFightStatTagsMustBeFishStats, CrossTableDistances, NewGearRowsNeedNoCode} | D/I | Every row of DT_Gear, DT_FightPattern, DT_FishFight against rules restated here (unique names the way FName compares, ranges, labels, starter free/shop priced, stats are Fish.Stat.* with DT_FishStat rows, TiredPull x ReelStrain >= SlackShare); 12 bad gear rows, 16 bad pattern rows and 20 bad tuning rows typed into CSV/JSON (plus non-finite values built in code and an unknown gear slot) are refused and fall back; every species' pattern valid; cross-table: shortest cast > LandDistance, longest cast <= MaxLineLength, spool > MaxLineLength; a new rod/line row works with no code |
| Authority | Authority.{ClientCannotLandOrSetTension, RequestsDuringFightDontReset} | I | A client copy can't hook, re-gear, cast, reel (its RPC is absorbed) or land; a forged copy is never simulated or landed and is overwritten by the next update; a cast/hook request during a fight is refused and the fight is untouched |
| Replication | Replication.{PropsReachOwnerAndProxies, FightStateRoundTrips, ProxyFollowsServerFight} | I | Loadout, FightNet, HookedFish, LastLandedFish, NetState are COND_None (owner and proxies); FLureFightNetState (68 bytes) and FLureGearLoadout round-trip; an owner copy and a simulated proxy follow three loadout changes (OnRep_Loadout re-resolves) and every fight update (bobber on the fish, HUD readout), never simulate or fire OnFishLanded |
| Landing | Landing.{OnFishLandedFiresOnce, LostFishNeverLands} | I | Exactly once per fish: re-entrant listener, two players, the AutoLandDelay switch mid-fight, one Catch: line each; snapped/spooled/thrown/reeled-in/line-cancelled fish never land, nothing left in hand, HUD text per outcome |
| Roll pipeline | Pipeline.{HookedFishFromTheRollPipeline, FightReadsFinalStats} | I/U | A natural bite's hooked fish IS FFishRoll's record (DecideBite and Roll replay it), the fight fish = MakeFish(record), seeded by FightSeed(record.Seed), landed unchanged; the fight reads the record, not the species row; 450 species x rarity x modifier x size x level records follow the spec formulas |
| Component | Component.{FrameRateIndependentOutcome, FightSuspendsBobberDistanceRule} | I | 30/60/120 fps worlds land the same fish on the same fixed step; beyond MaxLineLength during a fight the line stays, outside a fight it comes in (TooFar) |

### T-007 open bugs (failing tests; each test is the regression test for its bug)
- T007-B1 (minor) `Snap.ExactlyAtGraceHolds`, `Slack.ExactlyAtGraceHolds`: OverTime/SlackTime add float step times, so at exact multiples the
  "longer than the grace" check fires one step early: shipped SnapGraceTime 0.6 at 60 Hz snaps after 36 steps = exactly 0.600 s (OverTime
  0.600000083 > 0.6); also 0.5 s at 20 Hz, 0.25 s at 120 Hz, SlackGraceTime 0.5 at 60 Hz. Fix: count whole steps, or compare with half a
  step of tolerance. The implementer's `Fight.SnapAfterGraceNotBefore` check `OverBeforeSnap <= Grace` needs the same tolerance after the fix.
- T007-B2 (minor) `Data.PatternNeedsAPlainWeight`: `FLureFightPatternRow::Validate` accepts a pattern whose moves only have AggressionWeight
  (spec: "at least one move with a positive weight"). A calm fish (aggression 0, the DT_FishStat default) gets no move after the opening: it
  sulks at the tired pull and the HUD shows "Fish: None". Fix: require some move with Weight > 0.
- T007-B3 (minor) `Data.FishFightStatTagsMustBeFishStats`: `FLureFishFightRow::Validate` only checks the four stat tags are valid, not that
  they are Fish.Stat tags (its own message says so). StrengthStat = Bait.Shrimp imports cleanly and every fish pulls the 0.05 minimum.

### T-007 test updates after the B1-B3 fixes (qa-engineer, 2026-09-23)
- `Sim.StepMatchesSpecFormulas`: the oracle's snap and slack checks now count whole steps (round(t / dt) > grace / dt), like the B1 fix.
- `Fight.GearDecidesOutcome` (lead balance decision): part A (reference snapper, seeds 1-8) full reel snaps the starter line, the reef kit lands it; on the 7 kg snapper full reel snaps the starter line (and also the reef
  braid in 0.8 s: logged, for the lead); careful play lands with both kits, and the reef kit has a higher land rate or a shorter mean land time (seeds 1-8); the world run
  lands with both kits.
- T-006 tests updated to the T-007 design: `QA.Interrupt.TooFarFromTheBobber` (bobber distance rule enforced before Hooked, suspended in a
  fight), `QA.Net.ReplicatedToEveryone` (+FightNet, Loadout, COND_None), `QA.Net.ClientFollowsServerAtEveryStage` (Hooked: FightNet reaches
  the copy and the bobber rides on the fish; the rig's copy pawn is moved to the server pawn's spot). `QAFishingStateTest.cpp` and
  `QAFishingNetTest.cpp` no longer use a namespace-scope `using namespace`.

### T-007 observations (no failing test; for the lead)
- T007-O1 (balance, T-012): Rod_Reef's Drag 12 is above Line_Mono's strength 10, so letting a fish run can't protect the starter line. Prototype
  (independent Python transcription of the spec, 40 seeds): a careful player (reels < 70 %, eases off > 90 %) lands the reference Coral Snapper
  with the starter kit (13-17 s) but snaps it in 0.7 s with Rod_Reef + Line_Mono; a player who never reels during dives lands it with both.
  Upgrading the rod before the line makes the snapper harder. Options: effective drag = min(rod drag, share x line strength), or data.
- T007-O2 (design): during a move with little or no pull, reeling holds the tension at about RodPower x ReelLoad (1.2 on the starter rod), while
  "slack" is SlackShare x the fish's BasePull; for fish with BasePull above ~3.4 the line counts as slack even while you reel. No shipped species
  uses Dart (Charge has Pull 0); a Dart snapper would throw the hook in ~3.5 % of fights while the player holds reel (prototype). Also the
  heaviest Rare bonefish during rests below ~20 % stamina. The spec says "reel or it throws the hook".
- T007-O3 (minor exploit): the fight starts at the player-bobber distance at the hook; within LandDistance (150 cm) it is landed on the first step.
  Only reachable by walking to the bobber (e.g. wading at a shallow shore).
- T007-O4: the pattern fallback warns on every fight with an unknown/invalid FightPatternId (the table warnings are once per session); a species
  with FightPatternId None also warns every fight.
- T007-O5 (debug only): hooked with AutoLandDelay > 0, then switching the profile to AutoLandDelay 0 leaves the fish hooked forever (no fight begins).
- Review of the implementer's tests: they prove the headline acceptance (GearDecidesOutcome) but could pass with a broken fight in these ways:
  Spooled is never required (a missing spool check passes all 10); replication is a manual property copy (a COND_OwnerOnly regression passes);
  the snap/slack timing tolerances (+-2 steps) can't see the boundary; `HoldToReelInput` asserts `LineWhileReeling > 0` (always true) and accepts
  any result; the rod-bend check is skipped where the arms aren't imported (lanes); formula checks are spot values, not the integrated step.

### T-007 gaps (not covered by automation)
- Real network play: latency on ServerSetReeling, the owner's HUD under lag, bandwidth of FightNet updated every server tick (68 bytes full),
  2-player PIE: playtester. Iris replication is not enabled; the FRepLayout path is covered.
- Feel: fight length, snap speed, the tension bar, rod bend and line sag visuals, arms montages (empty slots until the animation-artist
  delivers): playtester and designer; Jimmy's open questions in the spec.
- The binary assets (DT_Gear, DT_FightPattern, DT_FishFight) don't exist until the editor-operator imports them in main; the settings path then
  replaces the built-in fallbacks. After the import, check once in main that the component resolves the imported tables (no fallback warning).

## T-027 fish anywhere + hot spots (lane eng1)
- Implementer: `Project.Fishing.Water.*` (FishingWaterTest/FishingHotSpotTest). 11 T-006 tests were updated to the new rules; QA reviewed each:
  all are legitimate rule updates (spot-less water now bites, NoSpecies path kept by empty fallbacks), none weakened.
- QA, `Tests/Fishing/QAFishingWaterTest.cpp` + `QAFishingHotSpotTest.cpp` (helpers `QAFishingWaterTestUtils.h`), all `Project.Fishing.Water.QA.*`:
  - Area selection (U): `Area.HigherPriorityWins`, `SmallerAreaWinsPriorityTie`, `LowerIdWinsFullTie`, `BrokenAreasNeverWin` (NaN/0/degenerate),
    `PolygonEdgeCases` (concave U, both windings), `BoxYawAndCircleEdge`, `DepthBands` ([min,max), max 0 = open), `DefaultWaterContext`.
  - Bite decision (U): `Bite.ShallowBoundaryAndReasonOrder` (14.9/15 cm, NotWater > TooShallow > gap > WrongBait, HUD text),
    `Bite.ContextSumsLuckAndCarriesBonus`, `Gap.FallbackOrderAndBait`, `Gap.LoggedOncePerHabitatAndHour`, `Gap.NoDeadWaterAnyHourShipped` (48 half-hours).
  - Roll (U): `Roll.NoBonusRollsUnchanged` (2000 seeded rolls, pinned fingerprint 2756817872: update only on an intended roll change),
    `Roll.SizeBonusFormula`, `Roll.ValueMultiplierFormula`.
  - World (I): `World.ShallowWaterNeverBitesAndSaysSo`, `OpenWaterBitesEverywhere`, `LegacySpotsOnlyWithoutAreas`, `AreaActorYawAndScale`.
  - Hot spots (U/I): `HotSpot.SpawnChanceAndLifetimeRoll`, `DriftDeterministicBoundedSmooth`, `SpawnRateFollowsSpawnInterval`, `MaxPerAreaAndLevelCap`,
    `LifetimeWithinRowThenGone`, `PrewarmFirstCheck`, `OnlyInValidWater`, `DriftNeverCrossesLand`, `BonusReachesTheBite`, `BonusKeptForWholeCast`,
    `OverlapNearestRelativeToRadius`.
  - Net (I, FTestWorlds): `Net2P.SpawnerHotSpotsAndBobberWaterReachClients` (same drift on server/client), `Net2P.DevCommandsOnClientAreSafe`.
  - Data (D): `Data.HotSpotRowsValid` (DT_HotSpot.json), `Data.HotSpotValidateCatchesBadRows`, `Data.LayoutWaterAreasFollowSchema` (every data/levels layout).
  - T-027b near-player rule (QA): `QA.HotSpot.FarAreaSpawnsOnlyWhenAPlayerComesNear` (far bounded area empty with players present, fills once one comes near); the Net2P test now places its area next to the measured pawn positions.

### T-027 open bugs (failing test = regression test)
- `HotSpot.DriftNeverCrossesLand`: the spawner checks only 8 points on the wander ring (spec rule), so a rock inside the drift disc between
  the points is accepted and the bubbles drift over land (a cast into them lands NotWater). Fix: sample the disc densely or stop drift over land.

### T-027 gaps
- /Game/Data/DT_HotSpot is not imported in the lane (built-in Bubbles fallback); after the import in main, check once that no fallback warning shows.
- Bubble/ripple visuals, the HUD hot-spot line and feel of spawn rates: playtester and designer. Real 2-player PIE latency: playtester.

## Rules
- Every new behavior gets at least one test written by someone other than its implementer (qa-engineer).
- Every gameplay DataTable gets a data-validation test (D) when it is created.
- Every bug fixed gets a regression test named after the bug.

## Dev tools (T-025, LIGHT gate)
- `Project.Dev.*` (8, implementer): teleport by marker/tag/spot id/XYZ, PlayerId, stance, GiveFish args + rolls, driver API.
- `Project.Dev.QA.ClientConsoleRefuses` (QA): on an NM_Client world Teleport and GiveFish refuse, nothing moves or rolls.
- Gap: a real networked client console (no replicated relay yet) is untested.
